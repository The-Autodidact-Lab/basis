"""
Top-level package for Cortex components.

This makes `cortex` importable so tests and other modules can use
`from cortex.context_cortex import cortex` and `from cortex import cortex_agent`.
"""


